<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="study.movieservice.repository.mapper.MemberMapper">
    <insert id="save" useGeneratedKeys="true" keyProperty="memberId">
        insert into member(email, login_id, login_password, nickname, grade)
        values (#{email}, #{loginId}, #{loginPassword},#{nickname},#{grade})
    </insert>

    <select id="findByLoginId" resultType="Member">
        select id, email, login_id, login_password, nickname, grade
        from member
        where login_id = #{loginId}
    </select>
</mapper>

